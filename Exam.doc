1. JS, express => HTTPS, CSR, SSR, middleware, API, rest, restful API
2. SQL => all method, group buy, join, subquery, having
3. Ex + SQL => connection pool, nodeJS folder structure, SQL injection, mysql2
4. Authentication:
- Hash
- JWT token:
           + Header: Algorithm, token JWT
           + Payload: token create time,
           + Signature: cách tạo

+ Access token
+ Refresh token